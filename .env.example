# Example environment configuration for nfa95 executable
# Place this file next to the executable and rename to ".env"

# Security (empty disables auth)
API_KEY=

# Network binding
HOST=0.0.0.0
PORT=8000

# General
TIMEZONE=Asia/Shanghai
RETENTION_DAYS=30
CONCURRENCY_LIMIT=3

# Optional custom directories (relative paths are resolved against the executable directory)
# STORAGE_DIR=storage
# LOG_DIR=logs

# MySQL connection for 95th percentile compute (recommended for production)
# If not set, the app will fallback to reading db_config.ini next to the executable
# MYSQL_HOST=127.0.0.1
# MYSQL_PORT=3306
# MYSQL_USER=
# MYSQL_PASSWORD=
# MYSQL_DB=
# MYSQL_CHARSET=utf8mb4
